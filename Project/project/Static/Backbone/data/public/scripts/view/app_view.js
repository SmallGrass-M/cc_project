// Generated by LiveScript 1.4.0
(function(){
  define(['views', 'collections', 'models', 'backbone'], function(views, collections, models){
    return Backbone.View.extend({
      model: new collections.user_collection(),
      initialize: function(){
        this.listenTo(this.model, 'add', this.addOne);
        this.listenTo(this.model, 'reset', this.addAll);
        return this.listenTo(this.model, 'all', this.render);
      },
      el: $('#UserCtrl'),
      render: function(){},
      events: {
        'click #getRemoteDate': 'getRemoteDate',
        'click .edit': 'edit'
      },
      addOne: function(item){
        var view;
        view = new views.user({
          model: item
        });
        return this.$el.find('ul').append(view.render().el);
      },
      addAll: function(){
        this.model.url = '/data';
        return this.model.fetch();
      },
      getRemoteDate: function(){
        this.addAll();
        return this.model.reset();
      }
    });
  });
}).call(this);
