// Generated by LiveScript 1.4.0
(function(){
  define(['collections', 'template', 'backbone', 'jquery'], function(collections, template){
    return Backbone.View.extend({
      model: {},
      initialize: function(){
        this.listenTo(this.model, 'change', this.render);
        return this.listenTo(this.model, 'destroy', this.remove);
      },
      tagName: 'li',
      render: function(){
        var model;
        model = this.model.toJSON();
        this.$el.html(template('user', model));
        return this;
      },
      events: {
        'click .edit': 'edit',
        'click .del': 'delete'
      },
      edit: function(){
        console.log(this.model.toJSON());
        this.model.set('name', this.$el.find('[name=name]').val());
        this.model.set('age', +this.$el.find('[name=age]').val());
        this.model.set('sex', this.$el.find('[name=sex]').val());
        return console.log(this.model.toJSON());
      },
      'delete': function(){
        return this.$el.remove();
      },
      clear: function(){
        return this.model.destory();
      },
      sync: function(key, val, opt){
        console.log(key, val);
        return console.log(opt);
      }
    });
  });
}).call(this);
